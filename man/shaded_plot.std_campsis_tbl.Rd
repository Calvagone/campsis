% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shaded_plot.R
\name{shaded_plot.std_campsis_tbl}
\alias{shaded_plot.std_campsis_tbl}
\title{Shaded plot for standard CAMPSIS simulation output.}
\usage{
\method{shaded_plot}{std_campsis_tbl}(
  x,
  variable = "CONC",
  colour = "auto",
  strat_extra = NULL,
  level = 0.9,
  alpha = 0.25,
  ...
)
}
\arguments{
\item{x}{a \code{std_campsis_tbl} object}

\item{variable}{name of the column to summarise on the y-axis. Defaults to
\code{"CONC"}. An informative error is raised when the column is absent.}

\item{colour}{stratification for line and ribbon colour. One of
\code{"auto"}, \code{NULL}, or a character vector of column names.
See Details.}

\item{strat_extra}{additional column name(s) used for stratification in the
prediction interval computation but \emph{not} mapped to colour — useful
when combined with \code{facet_wrap()}. Forwarded to
\code{\link{shadedPlot}}. Default is \code{NULL}.}

\item{level}{prediction interval level. Default is \code{0.90} (90\% PI).}

\item{alpha}{transparency of the ribbon. Default is \code{0.25}.}

\item{...}{additional arguments (currently unused, reserved for future use)}
}
\value{
a ggplot object
}
\description{
Plots a median line with a prediction interval ribbon from a
\code{std_campsis_tbl} (the default output of \code{simulate()} when no
custom \code{outfun} is used).
}
\details{
Colour stratification is resolved as follows:
\itemize{
  \item \code{"auto"} (default): \code{ARM} is added when more than one
        distinct arm is detected in the data; \code{SCENARIO} is added when
        more than one distinct scenario is detected.
  \item \code{NULL}: no colour mapping.
  \item character vector: explicit column name(s) to colour/fill by
        (forwarded directly to \code{\link{shadedPlot}}).
}
}
\seealso{
\code{\link{shadedPlot}}
}
